export type UnFormattedLocationData = {
   utc: string,
    battery_level: number,
    location_latitude: number,
    location_longitude: number,
}
    
export type location = {
    lat: number,
    lng: number,
}